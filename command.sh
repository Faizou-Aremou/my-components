# generate interface card
npx ng g i card --project=ui-controls
# generate standalone component
npx ng generate component --standalone copy-button --project=ui-controls
# generate applications
ng generate application ui-elements --defaults